import * as React from 'react';
import { render } from 'react-dom';
import { Everything } from './components';

const rootElement = document.getElementById("root");

render(<Everything />, rootElement);